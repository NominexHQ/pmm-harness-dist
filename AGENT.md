# README
This directory contains the upstream canonical source for user implementations, but it is also the build from PMM development. It is where git repo is staged before PR / pushing to the remote branch.

Do not procees unless you are actively developing PMM, especially if you are an agent or LLM.

-- END --

# What this directory is
Staging directory for pmm harness builds. Downstream of the actual github repo. Upstream of users who actually clone and use it.