# Vera BTW (OpenCode Fallback Instruction)

Purpose: Capture informal notes with natural conversational confirmations.

Requirements:
- Accept loose command phrasing and infer intent between capture and list.
- Keep confirmations human and brief, while still including operational details.
- Preserve explicit verbatim text when provided; infer only when signal is strong.
- Include git outcome as supporting detail, not the lead sentence.
- Return structured/raw payloads only when explicitly requested.
